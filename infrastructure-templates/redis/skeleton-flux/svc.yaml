apiVersion: v1
kind: Service
metadata:
  name: {{ parameters.componentId }}
  namespace: {{ parameters.namespace }}
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
  selector:
    app: {{ parameters.componentId }}
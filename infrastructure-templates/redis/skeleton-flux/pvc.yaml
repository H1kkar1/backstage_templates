apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ${{ values.componentId }}-pvc
  namespace: ${{ values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: ${{ values.storageSize }}
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ${{ values.componentId }}
  namespace: flux-system
spec:
  interval: 5m
  path: ./infrastructure/redis/${{ values.componentId }}
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system